\documentclass[../main.tex]{subfiles}
\begin{document}

\subsection*{Notes}

% ── Example definition ────────────────────────────────────────────────────────
% \begin{definition}[Vector Space]
%   A \emph{vector space} over $\F$ is a set $V$ together with addition and
%   scalar multiplication satisfying \dots
% \end{definition}

% ── Example theorem + proof ───────────────────────────────────────────────────
% \begin{theorem}
%   Every vector space has a unique additive identity.
% \end{theorem}
% \begin{proof}
%   \dots
% \end{proof}

% ── Useful macros reminder ────────────────────────────────────────────────────
% \F            → the field (Axler's notation)
% \R, \C        → real / complex numbers
% \Span         → span
% \Null, \Range → null space, range
% \inner{u}{v}  → ⟨u, v⟩

\end{document}
